section
    header
      h2#Forms Forms
    = fth_form_tag "#" do
      = component :form, :fieldset, legend: "Form legend" do
        = component :form, :formgroup do
          = fth_label_tag "email", "Email input:"
          = fth_text_field_tag "email", nil, type: "email", required: true, minlength: "5", placeholder: "test"
